#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pkcs7/lib/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../pkcs7/lib/cli.js" "$@"
  ret=$?
fi
exit $ret
